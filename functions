//Student info
let Students= [];
//Function cresteStudent()
function createStudent(name, studentNumber, marks, subject) {
return {
name: name,
sudentNumber: studentNumber,
marks: parseInt(marks), //convert to percentage
subject: subject,
grade: getGrade(marks), // get letter grade
status: getStatus(marks),//determain pass/fail status
}
};

function addStudent(Students) {
//To see if the list of students exists
if (typeof Students === 'undefined') {
Students = [];
}
//add Student
Students.push(student);
console.log( Student '{Student.name}' added succesfully!);
return Students;
}
